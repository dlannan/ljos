
-- @table f
return {
    table = require('f.table'), -- table: functional helpers to table type
    string = require('f.string'), -- table: functional helpers to string type
    p = require('f.table').print -- func: table print function
}